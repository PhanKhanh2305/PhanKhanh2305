@model IEnumerable<Mvc21BITV01MidTest.Data.TrangWeb>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenTrang)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Url)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenTrang)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Url)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaTrang">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaTrang">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaTrang">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
