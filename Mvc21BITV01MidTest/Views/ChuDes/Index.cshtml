@model IEnumerable<Mvc21BITV01MidTest.Data.ChuDe>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenCd)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaNvNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenCd)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaNvNavigation.MaNv)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaCd">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaCd">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaCd">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
